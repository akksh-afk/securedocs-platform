fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.7,<3
numpy>=1.26,<3
Pillow>=10,<13
# One OpenCV distribution only: mixing opencv-python / -headless / -contrib in one
# environment makes them overwrite each other's cv2 package.
opencv-contrib-python>=4.10
pytesseract>=0.3.13,<1
rapidfuzz>=3.9,<4
python-multipart>=0.0.9,<1
scikit-learn>=1.4
joblib>=1.3
# Runs the trained MRZ reader and forgery localiser. This installs the CPU build; for an NVIDIA
# GPU install the CUDA build instead, e.g. pip install torch --index-url https://download.pytorch.org/whl/cu128
torch>=2.4
pytest>=8,<9
# Optional: tests/test_face.py and generate_demo_documents.py use matplotlib's bundled sample photo.
matplotlib>=3.7
